

<!-- This example requires Tailwind CSS v2.0+ -->
<nav class="bg-gray-800">
    <div class="max-w-7xl mx-auto px-2 sm:px-6 lg:px-8">
      <div class="relative flex items-center justify-between h-16">
        <div class="absolute inset-y-0 left-0 flex items-center sm:hidden">
          <!-- Mobile menu button-->
          <button type="button" class="inline-flex items-center justify-center p-2 rounded-md text-gray-400 hover:text-white hover:bg-gray-700 focus:outline-none focus:ring-2 focus:ring-inset focus:ring-white" aria-controls="mobile-menu" aria-expanded="false">
            <span class="sr-only">Open main menu</span>
            <!--
              Icon when menu is closed.
  
              Heroicon name: outline/menu
  
              Menu open: "hidden", Menu closed: "block"
            -->
            <svg class="block h-6 w-6" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor" aria-hidden="true">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16" />
            </svg>
            <!--
              Icon when menu is open.
  
              Heroicon name: outline/x
  
              Menu open: "block", Menu closed: "hidden"
            -->
            <svg class="hidden h-6 w-6" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor" aria-hidden="true">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" />
            </svg>
          </button>
        </div>
        <div class="flex-1 flex items-center justify-center sm:items-stretch sm:justify-start">
          <div class="flex-shrink-0 flex items-center">
            
          </div>
          <div class="hidden sm:block sm:ml-6">
            <div class="flex space-x-4">
              <!-- Current: "bg-gray-900 text-white", Default: "text-gray-300 hover:bg-gray-700 hover:text-white" -->
              <a href="/" class="text-white px-3 py-2 rounded-md text-sm font-medium">STARTUP GUIDE</a>
              <a href="/event" class="bg-gray-900 text-white px-3 py-2 rounded-md text-sm font-medium">Events</a>
              <a href="/blogs" class="text-gray-300 hover:bg-gray-700 hover:text-white px-3 py-2 rounded-md text-sm font-medium">Blogs</a>
              <a href="/courses" class="text-gray-300 hover:bg-gray-700 hover:text-white px-3 py-2 rounded-md text-sm font-medium">Courses</a>
              <a href="/chatdb" class="text-gray-300 hover:bg-gray-700 hover:text-white px-3 py-2 rounded-md text-sm font-medium">Community Chat</a>
              
            </div>
          </div>
        </div>
        <div class="flex justify-end">
          <a href="/" class="text-gray-300 rounded-full bg-red-600 hover:bg-red-400 hover:text-white px-3 py-2 rounded-md text-sm font-medium">Log Out</a>
        </div>
        </div>
      </div>
    </div>
  </nav>
  








  <!-- component -->
<style>
    :root {
        --main-color: #4a76a8;
    }

    .bg-main-color {
        background-color: var(--main-color);
    }

    .text-main-color {
        color: var(--main-color);
    }

    .border-main-color {
        border-color: var(--main-color);
    }
</style>
<link href="https://unpkg.com/tailwindcss@^1.0/dist/tailwind.min.css" rel="stylesheet">
<script src="https://cdn.jsdelivr.net/gh/alpinejs/alpine@v2.x.x/dist/alpine.min.js" defer></script>



<div class="bg-gray-100">

            <nav :class="{'flex': open, 'hidden': !open}"
                class="flex-col flex-grow pb-4 md:pb-0 hidden md:flex md:justify-end md:flex-row">
              
                
            </nav>
        </div>
    </div>
    <!-- End of Navbar -->


<% const roomname = localStorage.getItem('ac'); %>















    <div class="container mx-auto my-5 p-5">
        <div class="md:flex no-wrap md:-mx-2 ">
            <!-- Left Side -->
            <div class="w-full md:w-3/12 md:mx-2">
                <!-- Profile Card -->
                <div class="bg-white p-3 border-t-4 border-green-400">
                   
                    <h1 class="text-gray-900 font-bold text-xl leading-8 my-1">
                    <% for(var i=0; i<hi.length; i++) {%>
                        <% const a = localStorage.getItem('name'); %>
                        <% const b = JSON.parse(a); %>
                        <% const name = (b.name); %>
                            <% if(hi[i].firstname == name ){ %>
                                <%= hi[i].firstname %>
                             <% } else { %>
                             <% } %>
                    <% } %>
                    </h1>
                    <h3 class="text-gray-600 font-lg text-semibold leading-6">Owner at Her Company Inc.</h3>
                    <p class="text-sm text-gray-500 hover:text-gray-600 leading-6">     <% for(var i=0; i<hi.length; i++) {%>
                        <% const a = localStorage.getItem('name'); %>
                        <% const b = JSON.parse(a); %>
                        <% const bintro = (b.bintro); %>
                            <% if(hi[i].bintro == bintro ){ %>
                                <%= hi[i].bintro %>
                             <% } else { %>
                             <% } %>
                    <% } %></p>
                    <ul
                        class="bg-gray-100 text-gray-600 hover:text-gray-700 hover:shadow py-2 px-3 mt-3 divide-y rounded shadow-sm">
                        <li class="flex items-center py-3">
                            <span>Status</span>
                            <span class="ml-auto"><span
                                    class="bg-green-500 py-1 px-2 rounded text-white text-sm">Active</span></span>
                        </li>
                        <li class="flex items-center py-3">
                            <span>Member since</span>
                            <span class="ml-auto">Nov 07, 2016</span>
                        </li>
                        <li class="flex items-center py-3">
                            <div class="mx-auto">
                                <a href="/leaveamessage" class="text-gray-300 rounded-full bg-red-600 hover:bg-red-400 hover:text-white px-3 py-2 rounded-md text-sm font-medium">Leave a Message</a>
                            </div>
                            <div class="mx-auto">
                              <% for(var i=0; i<hi.length; i++) {%>
                        <% const a = localStorage.getItem('name'); %>
                        <% const b = JSON.parse(a); %>
                        <% const name = (b.name); %>
                            <% if(hi[i].firstname == name ){ %>

                            <% const a = `"${name}"` %>
                             
                                <a href="/chat.html?username=<%= roomname %>&room=<%= a %>" class="text-gray-300 rounded-full bg-green-600 hover:bg-green-400 hover:text-white px-3 py-2 rounded-md text-sm font-medium">Live Chat</a>
                                
                             
                            </div>
                            <% } %>
                    <% } %>
                            
                            
                        </li>
                    </ul>
                </div>
                <!-- End of profile card -->
                <div class="my-4"></div>
                <!-- Friends card -->
                
                <!-- End of friends card -->
            </div>
            <!-- Right Side -->
            <div class="w-full md:w-9/12 mx-2 h-64">
                <!-- Profile tab -->
                <!-- About Section -->
                <div class="bg-white p-3 shadow-sm rounded-sm">
                    <div class="flex items-center space-x-2 font-semibold text-gray-900 leading-8">
                        <span clas="text-green-500">
                            <svg class="h-5" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24"
                                stroke="currentColor">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                    d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z" />
                            </svg>
                        </span>
                        <span class="tracking-wide">About</span>
                    </div>
                    <div class="text-gray-700">
                        <div class="grid md:grid-cols-2 text-sm">
                            <div class="grid grid-cols-2">
                                <div class="px-4 py-2 font-semibold">First Name</div>
                                <div class="px-4 py-2">
                                  <% for(var i=0; i<hi.length; i++) {%>
                        <% const a = localStorage.getItem('name'); %>
                        <% const b = JSON.parse(a); %>
                        <% const name = (b.name); %>
                            <% if(hi[i].firstname == name ){ %>
                                <%= hi[i].firstname %>
                             <% } else { %>
                             <% } %>
                    <% } %></div>
                            </div>
                            <div class="grid grid-cols-2">
                                <div class="px-4 py-2 font-semibold">Last Name</div>
                                <div class="px-4 py-2"><% for(var i=0; i<hi.length; i++) {%>
                        <% const a = localStorage.getItem('name'); %>
                        <% const b = JSON.parse(a); %>
                        <% const lastname = (b.lastname); %>
                            <% if(hi[i].lastname == lastname ){ %>
                                <%= hi[i].lastname %>
                             <% } else { %>
                             <% } %>
                    <% } %></div>
                            </div>
                            
                            
                            <div class="grid grid-cols-2">
                                <div class="px-4 py-2 font-semibold">Contact No.</div>
                                <div class="px-4 py-2"><% for(var i=0; i<hi.length; i++) {%>
                        <% const a = localStorage.getItem('name'); %>
                        <% const b = JSON.parse(a); %>
                        <% const contact = (b.contact); %>
                            <% if(hi[i].contact == contact ){ %>
                                <%= hi[i].contact %>
                             <% } else { %>
                             <% } %>
                    <% } %></div>
                            </div>
                            <div class="grid grid-cols-2">
                                <div class="px-4 py-2 font-semibold">Address</div>
                                <div class="px-4 py-2">Beech Creek, PA, Pennsylvania</div>
                            </div>
                          
                            <div class="grid grid-cols-2">
                                <div class="px-4 py-2 font-semibold">Email.</div>
                                <div class="px-4 py-2">
                                    <a class="text-blue-800" href="mailto:jane@example.com">
                                    <% for(var i=0; i<hi.length; i++) {%>
                        <% const a = localStorage.getItem('name'); %>
                        <% const b = JSON.parse(a); %>
                        <% const email = (b.email); %>
                            <% if(hi[i].email == email ){ %>
                                <%= hi[i].email %>
                             <% } else { %>
                             <% } %>
                    <% } %></a>
                                </div>
                            </div>
                            <div class="grid grid-cols-2">
                                <div class="px-4 py-2 font-semibold">Birthday</div>
                                <div class="px-4 py-2">Feb 06, 1998</div>
                            </div>
                            <div class="grid grid-cols-2">
                              <div class="px-4 py-2 font-semibold">City</div>
                              <div class="px-4 py-2"><% for(var i=0; i<hi.length; i++) {%>
                        <% const a = localStorage.getItem('name'); %>
                        <% const b = JSON.parse(a); %>
                        <% const city = (b.city); %>
                            <% if(hi[i].city == city ){ %>
                                <%= hi[i].city %>
                             <% } else { %>
                             <% } %>
                    <% } %></div>
                          </div>
                          <div class="grid grid-cols-2">
                            <div class="px-4 py-2 font-semibold">State</div>
                            <div class="px-4 py-2"><% for(var i=0; i<hi.length; i++) {%>
                        <% const a = localStorage.getItem('name'); %>
                        <% const b = JSON.parse(a); %>
                        <% const state = (b.state); %>
                            <% if(hi[i].state == state ){ %>
                                <%= hi[i].state %>
                             <% } else { %>
                             <% } %>
                    <% } %></div>
                        </div>
                        <div class="grid grid-cols-2">
                          <div class="px-4 py-2 font-semibold">Area of Expertise</div>
                          <div class="px-4 py-2"><% for(var i=0; i<hi.length; i++) {%>
                        <% const a = localStorage.getItem('name'); %>
                        <% const b = JSON.parse(a); %>
                        <% const areaofexpertise = (b.areaofexpertise); %>
                            <% if(hi[i].areaofexpertise == areaofexpertise ){ %>
                                <%= hi[i].areaofexpertise %>
                             <% } else { %>
                             <% } %>
                    <% } %></div>
                      </div>
                      
                        </div>
                        
                    </div>
                    <div class="grid grid-cols-4">
                      <div class="px-4 py-2 font-semibold">About me</div>
                      <div class="col-span-3 my-12 mx-4 pr-8 overflow-y-auto text-justify"><% for(var i=0; i<hi.length; i++) {%>
                        <% const a = localStorage.getItem('name'); %>
                        <% const b = JSON.parse(a); %>
                        <% const dintro = (b.dintro); %>
                            <% if(hi[i].dintro == dintro ){ %>
                                <%= hi[i].dintro %>
                             <% } else { %>
                             <% } %>
                    <% } %></div>
                  </div>
              
                    
                </div>
                <!-- End of about section -->

                <div class="my-4"></div>

                <!-- Experience and education -->
                <div class="bg-white p-3 shadow-sm rounded-sm">

                    <div class="grid grid-cols-2">
                        <div>
                            <div class="flex items-center space-x-2 font-semibold text-gray-900 leading-8 mb-3">
                                <span clas="text-green-500">
                                    <svg class="h-5" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24"
                                        stroke="currentColor">
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                            d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z" />
                                    </svg>
                                </span>
                                <span class="tracking-wide">Experience</span>
                            </div>
                            <ul class="list-inside space-y-2">
                                <li>
                                    <div class="text-teal-600"><% for(var i=0; i<hi.length; i++) {%>
                        <% const a = localStorage.getItem('name'); %>
                        <% const b = JSON.parse(a); %>
                        <% const experience = (b.experience); %>
                            <% if(hi[i].experience == experience ){ %>
                                <%= hi[i].experience %>
                             <% } else { %>
                             <% } %>
                    <% } %></div>
                         
                        </div>
                        <div>
                            <div class="flex items-center space-x-2 font-semibold text-gray-900 leading-8 mb-3">
                                <span clas="text-green-500">
                                    <svg class="h-5" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24"
                                        stroke="currentColor">
                                        <path fill="#fff" d="M12 14l9-5-9-5-9 5 9 5z" />
                                        <path fill="#fff"
                                            d="M12 14l6.16-3.422a12.083 12.083 0 01.665 6.479A11.952 11.952 0 0012 20.055a11.952 11.952 0 00-6.824-2.998 12.078 12.078 0 01.665-6.479L12 14z" />
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                            d="M12 14l9-5-9-5-9 5 9 5zm0 0l6.16-3.422a12.083 12.083 0 01.665 6.479A11.952 11.952 0 0012 20.055a11.952 11.952 0 00-6.824-2.998 12.078 12.078 0 01.665-6.479L12 14zm-4 6v-7.5l4-2.222" />
                                    </svg>
                                </span>
                                <span class="tracking-wide">Education</span>
                            </div>
                            <ul class="list-inside space-y-2">
                                <li>
                                    <div class="text-teal-600"><% for(var i=0; i<hi.length; i++) {%>
                        <% const a = localStorage.getItem('name'); %>
                        <% const b = JSON.parse(a); %>
                        <% const education = (b.education); %>
                            <% if(hi[i].education == education ){ %>
                                <%= hi[i].education %>
                             <% } else { %>
                             <% } %>
                    <% } %></div>
                                    <div class="text-gray-500 text-xs">March 2020 - Now</div>
                                </li>
                              
                            </ul>
                        </div>
                    </div>
                    <!-- End of Experience and education grid -->
                </div>
                <!-- End of profile tab -->


                
            </div>
        </div>
    </div>
    
</div>